version: "3.7"

# instead use docker-compose --env-file option?

services:
  odoo:
    restart: "no"
    environment:
      - DB_NAME_TEST=test
      - TEST_ENABLE=True
      - LOG_LEVEL=debug
      - WITHOUT_DEMO=False
      - PIP_AUTO_INSTALL=1
